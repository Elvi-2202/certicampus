{# ============================================================ #}
{# templates/admin/specialities/index.html.twig                 #}
{# ============================================================ #}
{% extends 'base.html.twig' %}
{% import 'admin/macros.html.twig' as ui %}
{% block title %}Spécialités — Certicampus{% endblock %}
{% block stylesheets %}{{ ui.sidebar('specialities') | raw }}{% endblock %}
{% block body %}
<div class="layout">
  {{ ui.sidebar('specialities') | raw }}
  <main class="main">
    <div class="page-header">
      <div><h1>Spécialités</h1><p>{{ specialities|length }} spécialité(s)</p></div>
      <a href="{{ path('app_admin_specialities_new') }}" class="btn">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        Nouvelle spécialité
      </a>
    </div>
    <div class="search-bar"><input type="text" id="search" placeholder="Rechercher..." onkeyup="filterTable()"></div>
    <div class="table-wrap">
      {% if specialities is empty %}<div class="empty">Aucune spécialité enregistrée.</div>
      {% else %}
      <table id="table">
        <thead><tr><th>#</th><th>Nom</th><th>Description</th><th>Actions</th></tr></thead>
        <tbody>
        {% for s in specialities %}
        <tr>
          <td style="color:#aaa;">{{ s.id }}</td>
          <td><strong>{{ s.label }}</strong></td>
          <td style="color:#555;">{{ s.description|default('—')|slice(0,80) }}{% if s.description|length > 80 %}...{% endif %}</td>
          <td><div class="actions">
            <a href="{{ path('app_admin_specialities_edit', {id: s.id}) }}" class="btn btn-outline btn-sm">Modifier</a>
            <form method="POST" action="{{ path('app_admin_specialities_delete', {id: s.id}) }}" onsubmit="return confirm('Supprimer ?')">
              <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s.id) }}">
              <button type="submit" class="btn btn-danger btn-sm">Supprimer</button>
            </form>
          </div></td>
        </tr>
        {% endfor %}
        </tbody>
      </table>
      {% endif %}
    </div>
  </main>
</div>
<script>function filterTable(){const q=document.getElementById('search').value.toLowerCase();document.querySelectorAll('#table tbody tr').forEach(r=>r.style.display=r.textContent.toLowerCase().includes(q)?'':'none');}</script>
{% endblock %}